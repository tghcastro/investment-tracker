meta {
  name: investment-tracker-api
}

docs {
  Local REST API for bond portfolio tracker (M1–M3).
  Start server: packages/api `npm run dev` or root `npm run dev:api`.
  Select environment: local.
  Suggested flow: Create Account → Create Holding → Create Coupon Payment → Income Summary / Upcoming Coupons.
}
