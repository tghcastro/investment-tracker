meta {
  name: Create Account (invalid — missing name)
  type: http
  seq: 4
}

post {
  url: {{baseUrl}}/api/accounts
  body: json
  auth: none
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "description": "Should fail validation"
  }
}
